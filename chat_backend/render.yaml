services:
  - type: web
    name: bubble-chat-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      # Add your environment variables in Render dashboard
      # MONGODB_URL, JWT_SECRET, SMTP settings, etc.

